# 播放链路

## 用户流程与界面

点击番剧直接打开 `/#/watch/:animeId`，不再打开详情弹窗。在同一个页面中选择来源条目、该条目的线路和对应分集；点击分集解析媒体，使用视频控件开始播放。换来源或线路立即卸载旧播放器并清空旧分集选择，不按数组下标跨来源猜测相同集数。下一集只在当前线路内移动。

延续发现页的 Codex 式侧栏、工具栏和留白。亮色采用白 `#FFFFFF`、雾灰 `#F7F7F8`、炭灰 `#252628` 与鼠尾草绿 `#447467`；暗色画布 `#202123`。播放器为独立深色画面区 `#101112`，媒体 token 与 CSS 同步。系统字体，左对齐，标题 27px，分区标题 15px。桌面为大播放器＋右侧来源/分集栏；窄屏顺序为播放器、操作条、来源/分集、简介。设计复核后保留现有语言，不添加无实际用途的弹幕、下载或清晰度占位按钮。

```text
侧栏 | 返回发现 / 番剧名称                  加入追番
     | 播放器                         | 来源搜索与候选
     | 当前集 / 重新解析 / 下一集        | 线路 / 独立分集列表
     | 番剧资料与简介                  |
```

主题支持亮色、暗色、系统；保留可见焦点、原生视频键盘操作与减少动态效果偏好。没有来源、没有分集、请求失败、媒体错误分别显示恢复入口。来源原始名称通过 React 文本渲染，不插入第三方 HTML 或执行第三方脚本。

## 服务职责

- `apps/server`：Fastify JSON API、站点 adapter、HTML/JSON 解析、受限网络宿主和媒体网关；不做 SSR。
- `source-engine`：来源/条目/线路/分集与 repository 契约、精确标题匹配和排序；尚未实现 AniBaka 通用规则解释器。
- `api-client`：Hana HTTP 客户端及 `unknown` 响应校验；Bangumi 仍仅提供元数据。
- `feature-core`：取消过期请求、来源/分集/解析状态与本地历史；不访问 DOM 或浏览器存储。
- `apps/web/src/playback`：页面、hash 地址适配和 HTMLVideoElement/HLS.js 的 `PlayerAdapter` 实现。Desktop/Native 仍为占位。

## 当前来源

| 来源               | 实现范围                                                                                       | 已知限制                                                                                                                                            |
| ------------------ | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 稀饭动漫           | 搜索、详情公开序列化数据解析、默认原生 HLS 分集、播放地址签发                                  | 当前适配 `xfy*` 线路对应的默认 HLS；不将外部备用线路伪装为同一地址。发布 key 来自站点公开客户端，不是 service-role 密钥；站点变更可能需要更新适配器 |
| Anime7             | suggest 搜索、HTML 线路/分集、`player_aaaa` JSON 和 URL/base64 编码解析，支持直接 HLS/MP4/WebM | 不执行 JS 嗅探或额外解析站；部分媒体 CDN 存在地区限制                                                                                               |
| TvTFun（默认关闭） | 搜索、线路/分集与旧版 resolve 流程的适配代码                                                   | 实测播放凭证要求已变更，当前不能确认可播；仅进一步调试时用 `HANA_ENABLE_TVTFUN=1`，不作为默认可用来源                                               |

适配依据来自只读参考规则和实际公开响应；未修改参考项目。来源可能随时间、地区和上游策略变化；某集成功不代表全站可用。失败不回退到测试视频或虚构结果。候选标题保留，用户可修改关键词重新搜索；带元数据 ID 的结果必须 ID 相同才算精确匹配，同名但不同 ID 仍为候选。

## API

| 方法与路径                   | 输入                                   | 输出                                               |
| ---------------------------- | -------------------------------------- | -------------------------------------------------- |
| GET `/api/health`            | —                                      | 服务状态                                           |
| GET `/api/sources`           | —                                      | 注册来源信息                                       |
| GET `/api/anime/:id`         | Bangumi ID                             | 校验后的 Anime；本地精选标记 snapshot，其他 online |
| GET `/api/playback/search`   | animeId、title、originalTitle          | 每个来源的候选及独立错误；某源失败保留其他结果     |
| GET `/api/playback/episodes` | sourceId、subjectId                    | 此条目的线路与各线路分集                           |
| GET `/api/playback/resolve`  | sourceId、subjectId、lineId、episodeId | 校验分集归属后返回临时媒体票据地址                 |
| GET `/api/media/:token`      | 已签发的票据；可带单区间 Range         | HLS 清单、子清单、密钥/初始化片段或视频流          |

API 请求有参数校验、速率限制、超时和客户端断开取消。分集缓存 5 分钟，最多 128 条。解析不缓存签名地址，以便重新解析恢复过期资源。媒体票据内存保存，最长 6 小时、最多 20,000 个；服务重启使票据失效，可重新解析。上游签名可能更早过期，页面提供重新解析。

媒体接口不接受任意 URL，仅使用 adapter 签发的随机票据。连接时检查 DNS 公网地址，重定向逐跳检查；拒绝内网、凭证 URL、非 HTTP(S) 和非常规端口。HLS 重写主/子清单、分片、KEY、MAP、MEDIA 等 URI，跨域子资源不继承 Cookie/Authorization/apikey。Range、Content-Range、Content-Length 按流转发，媒体错误不产生伪造文件。没有转码、DRM 或验证码自动化支持。

## 运行与部署

```sh
pnpm install
pnpm dev                         # Vite 5173 + Fastify 3001
pnpm dev:web                     # 仅前端
pnpm dev:server                  # 仅服务端
pnpm build                       # 两端生产构建
pnpm --filter @hanacg/server start
```

服务端读取 `PORT`（默认 3001）、`HOST`（默认 127.0.0.1）。生产部署前端静态目录 `apps/web/dist`，API 运行 `apps/server/dist/index.js` 并保留安装后的服务端依赖；在反向代理上将同域 `/api/*` 转发到 Node。媒体代理关闭响应缓冲，保留 Range；Vite preview 仅预览静态前端，不配置生产代理。媒体转发占用服务端带宽。

开发环境若使用 Fake-IP DNS，可显式设置 `HANA_FAKE_IP_DNS=1 pnpm dev`。此开关仅兼容域名解析后的 `198.18.0.0/15` 映射，不允许客户端直接请求保留 IP；只有在信任本地代理会安全处理外部域名时启用，生产默认关闭。这里不自动修改系统网络设置。

## 地址与本地记录

hash 查询参数 source/subject/line/episode 标识当前选择，不含播放 URL。刷新会重新匹配和解析；旧媒体票据不会保存到本地。进入页面不会新增观看记录。开始播放后每约 5 秒、暂停/结束、页面隐藏和离开时保存进度。

`hana:history:v1` 保存 `{ version: 1, entries: [...] }`，最多 100 部、每部保留最新一次记录；读取验证版本、结构、ID 与有限非负数值，损坏数据回到空状态。通过存储 adapter 写入失败时给出会话提示。恢复只使用同来源、同条目、同线路、同分集的进度，临近结束时从头开始；不跨来源猜测进度，不提供账户同步。

## 验证

- `pnpm check`：Web/服务端与共享包类型检查、来源/网络/网关/历史单测和两端构建。
- `pnpm test:e2e`：Chrome 中受控 API、浏览器生成 WebM 的实际解码/播放、换源、换线路、下一集、失败/空态、取消过期响应、历史与刷新、亮暗和窄屏截图。测试视频仅存在于测试拦截器。
- `node scripts/verify-live-playback.mjs`：开发服务运行后显式执行真实来源冒烟，验证指定分集的画面和进度；不纳入离线 CI。

2026-09-15 的真实验证：稀饭动漫《葬送的芙莉莲》第 1 集经 Fastify → HLS.js 在 Chrome 播放，画面尺寸非零、播放进度超过 2 秒、时长约 1561 秒，无页面脚本错误；主/子清单均重写成功，分片 Range 返回 206。Anime7 搜索、分集与地址解析成功，但测试媒体 CDN 对当前出口返回地区限制；TvTFun 搜索/分集成功，解析返回凭证错误。真实结果与受控测试分别报告。
